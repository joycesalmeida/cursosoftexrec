class complexo:
    def __init__(self, real, imag);
        self.real = real
        self.imag = imag

    def somar(self, outroNumero);
        self.real = self.real + outroNumero.real
        self.imag = self.imag + outroNumero.imag
    
    def imprimir(self);
        return "{} + {}i".format(elf.real, self.imag)

def somaComplexo(v1, v2);
    real = v1.real + v2.real
    imag = v1.imag + v2.imag

    resultado = Complexo(real, imag)

    return resultado

c = Complexo(2,4) # 2 + 41
j = complexo(5,2) # 5+21

print(c.imprimir())
print(j.imprimir())

c.somar(j)
print(c.imprimir())